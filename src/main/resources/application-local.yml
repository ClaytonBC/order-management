spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/order_management_db
    username: postgres
    password: 123456

  jpa:
    hibernate:
      ddl-auto: update

  h2:
    console:
      enabled: true